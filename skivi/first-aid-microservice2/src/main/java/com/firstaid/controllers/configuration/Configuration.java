package com.firstaid.controllers.configuration;

/**
 * Aceasta clasa se ocupa cu parsarea unui fisier in format JSON
 * ce se afla in resurse.
 */
public class Configuration {
    public static final String DEPLOYMENT_MODE = "local";
}
